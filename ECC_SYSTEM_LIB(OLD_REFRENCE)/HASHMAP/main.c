#include"HASHMAP_IMPL.h"
/*
int main()
{
	struct HashMap* mp = malloc(sizeof(struct HashMap));
	HASHMAP_initHashMap(mp);
	HASHMAP_insert(mp, "jonathan", "peri");
	printf("%s\n", HASHMAP_search(mp,"jonathan"));
}
*/
/*
int count;
struct HASHMAP_Entry* entries = HASHMAP_getAllElements(&mp, &count);
for (int i = 0; i < count; i++)
{
    printf("%s: %s\n", entries[i].key, entries[i].value);
}
free(entries);
*/